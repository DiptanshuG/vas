"use client";

import styled from "@emotion/styled";

export const StyledCard = styled.div`
  padding: 1rem;
  border-radius: 24px;
  border: 1px solid var(--Stroke, #eaecf0);
  background: var(--White-0, #fff);
  box-shadow: 0px 24px 43.3px 0px rgba(0, 0, 0, 0.04);
`;
